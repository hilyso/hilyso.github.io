---
title: Kubernetes Binary Deployment
date: 2026-09-014 16:00:00
categories: 
- [K8S]
tags: 
- K8s


一、 下面配置在 K8s 三台主机
K8s-master K8s-node01 K8s-node02 

1) 修改主机名
hostnamectl set-hostname K8s-master
2) 开启内核参数
modprobe br_netfilter overlay
   cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
   overlay
   br_netfilter
   EOF
3）关闭交换分区
swapoff -a
   sed -ri 's/.*swap.*/#&/' /etc/fstab
4) 关闭防火墙
systemctl stop firewalld iptables
   systemctl disable firewalld iptables
5) 关闭SELinux
setenforce 0
   sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config
6) 开启时间同步
systemctl enable chronyd --now

8）开启路由转发

``` bash
cat >> /etc/sysctl.d/k8s.conf << EOF
net.ipv4.ip_forward = 1
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
EOF
```

9）SSH免密登录
ssh-keygen
ssh-copy-id root@K8s-node01
ssh-copy-id root@K8s-node02
10）HOSTS 解析
cat >> /etc/hosts << EOF
192.168.100.115 K8s-master
192.168.100.116 K8s-node1
192.168.100.117 K8s-node2
EOF
11) 安装必要工具
yum install -y wget curl tar vim socat conntrack ipset ipvsadm bash-completion


二、 证书
etcd 和 K8s 集群使用不同 CA

1）目录
/etc/etcd/ssl/                # etcd 相关证书
/etc/kubernetes/ssl/          # k8s 组件证书（apiserver、controller-manager、scheduler、admin、front-proxy等）
/etc/kubernetes/ssl/          # kubelet 证书
~/.kube/config                # kubectl 使用的 kubeconfig（admin证书生成）
/etc/kubernetes/config/       # 各组件配置文件、kubeconfig（*.kubeconfig）


2) 创建 CA

etcd  和 k8s 需要分别创建不同的 CA
使用 cfssl 可以快速创建


` cfssl gencert -initca ca-csr.json | cfssljson -bare ca `


```
cat > ca-config.json <<EOF
{
  "signing": {
    "default": {
      "expiry": "87600h"
    },
    "profiles": {
      "server": {
        "expiry": "87600h",
        "usages": ["signing","key encipherment","server auth"]
      },
      "client": {
        "expiry": "87600h",
        "usages": ["signing","key encipherment","client auth"]
      },
      "peer": {
        "expiry": "87600h",
        "usages": ["signing","key encipherment","server auth","client auth"]
      }
    }
  }
}
EOF
```

```
cat > ca-csr.json <<EOF
{
  "CN": "etcd-ca",
  "key": {"algo": "rsa", "size": 2048},
  "names": [
    {"C": "CN", "ST": "Beijing", "L": "Beijing", "O": "etcd", "OU": "etcd"}
  ]
}
EOF
```

